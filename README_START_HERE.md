# Hifz Planner - Start Here

This is your main Quran app project.

Open this workspace for normal work:

```bash
code /home/fa507/dev/hifz_planner-only.code-workspace
```

Ignore these at first:
- `/home/fa507/dev/references/` is archive/reference material, not your daily workspace.
- `agent.md`, `APP_KNOWLEDGE.md`, and `docs/assistant/` are deeper docs. Use them only when you need extra detail.

Your first 3 actions in VS Code:
1. Open the Explorer and confirm you only see `hifz_planner`.
2. Open `README.md` and use it as the main landing page.
3. Open `lib/app/router.dart` to see the main app areas and where each screen lives.

If the environment or tools feel confusing:
- open `docs/assistant/LOCAL_CAPABILITIES.md` to see what is actually available right now
- open `docs/assistant/LOCAL_ENV_PROFILE.example.md` to understand when to prefer WSL or Windows

Safest next command, only if you need it:

```bash
git status --short --branch
```

That command only shows your current repo state. It does not change anything.
